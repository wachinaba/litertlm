name: litertlm_example
description: "litertlm example"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  litertlm:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
