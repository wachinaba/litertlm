name: Android checks

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter --version
      - run: flutter pub get
      - name: Prepare declared example asset directories
        run: mkdir -p example/assets/audio example/assets/images example/assets/models
      - run: flutter analyze --no-fatal-infos --no-fatal-warnings
      - run: flutter test
      - name: Generate Android example runner
        run: |
          flutter create --platforms=android --project-name=litertlm_example --org=org.rockstudio example
      - name: Compile Android bridge
        working-directory: example
        run: flutter build apk --debug
